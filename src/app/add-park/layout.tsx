
export default function AddParkLayout ({
    children,
  }: {
    children: React.ReactNode
  }) {

    return (
        <div className=" w-full lg:py-8">
            {children}
        </div>
    )
  }